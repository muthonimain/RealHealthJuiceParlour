# Optional — PostgreSQL is no longer used.
# Kept as a stub so `docker compose up` does not fail if someone still runs it.

services:
  noop:
    image: alpine:3.20
    command: ["true"]
    profiles: ["unused"]
